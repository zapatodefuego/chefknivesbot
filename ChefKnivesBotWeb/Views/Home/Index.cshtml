@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <input type="button" value="Login" onclick="@("window.location.href='" + @Url.Action("SignIn", "Authentication") + "'");" />
    </div>
